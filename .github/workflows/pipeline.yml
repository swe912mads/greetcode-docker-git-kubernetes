name: Build Docker Image for Python GreetCode

on:
  push:
    branches:
      - main
      - f4

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_swethamathiyalagan }}
          password: ${{ secrets.DOCKER_mathi944434 }}

      - name: Build Docker Image
        run: docker build -t python-greetcode .